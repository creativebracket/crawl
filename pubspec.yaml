name: crawl
description: A cli for creating pubspec.yaml files.
version: 0.1.0
homepage: https://github.com/creativebracket/crawl

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  args: ^1.6.0
  dcli: ^0.25.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4

executables:
  crawl:
